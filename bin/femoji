#!/bin/sh
#
curl -s https://unicode.org/Public/emoji/latest/emoji-test.txt |
    sed -e '/^#/d' \
        -e '/^$/d' \
        -e 's/^.*# //' \
        -e 's/E[^ ]* //' |
    fzf --ansi --no-info | cut -d' ' -f1
